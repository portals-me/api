#set ( $items = [] )

#foreach ( $item in $context.result.items )
  #set ( $result = $util.map.copyAndRemoveAllKeys($item, [ "id", "sort_id" ]) )
  #set ( $result.collectionId = $item.id )
  #set ( $result.id = $item.sort_id.replace("article@@", "") )

  $util.qr( $items.add( $result ) )
#end

$utils.toJson($items)
